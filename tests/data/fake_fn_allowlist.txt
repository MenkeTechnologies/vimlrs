# fake_fn_allowlist.txt — sanctioned `fn` names under src/ported/ that are NOT
# in the csrc-derived list (docs/nvim_c_functions.txt).
#
# Ported from zshrs's tests/data/fake_fn_allowlist.txt + ported_fn_names_match_c.
# The test `tests/ported_fn_names_match_c.rs` rejects any fn under src/ported/
# whose name is neither in docs/nvim_c_functions.txt NOR listed here NOR a
# trait-impl / #[test] exemption. Adding a name here is the audit trail for an
# exception — keep each justified.
#
# Every entry below is a REAL Neovim function whose defining C file is not
# vendored under csrc/ (only the eval/ tree is). They are faithful ports of real
# Vim builtins/helpers, cited to their home file — analogous to the already-
# recognized extern deps emsg/vim_str2nr (those appear as calls in the eval tree,
# so they land in the generated list; these do not). There are NO synthesis
# adapters here: those have been inlined or expressed as `From`/trait impls.
# When math.c / strings.c / option.c get vendored, move their names out of this
# file and the generator will recognize them.

# ── strings.c (not vendored) ──
f_string
f_strlen
f_strchars
f_strgetchar
f_strcharpart
f_byteidx
f_byteidxcomp
f_charidx
f_strpart
f_stridx
f_strridx
f_tolower
f_toupper
f_tr
f_trim
f_str2nr
f_str2list
f_slice
f_strcharlen
f_strtrans
f_strwidth
f_strdisplaywidth
f_charclass
utf_iscomposing
utf_char2cells

# ── option.c (not vendored) ──
do_set
findoption
set_option_value

# ── sha256.c (not vendored) ──
# Real Neovim SHA-256 helpers behind sha256(). Only sha256_bytes is called from
# the vendored eval tree (so the generator already recognizes it); these four
# are called only within sha256.c.
sha256_start
sha256_process
sha256_update
sha256_finish

# ── path.c (not vendored) ──
# Real Neovim path helpers behind pathshorten(). The others (vim_ispathsep,
# get_past_head, path_tail, shorten_dir_len) appear as calls in the vendored
# eval tree so the generator already recognizes them; vim_ispathsep_nocolon is
# only called from within path.c, so it is listed here.
vim_ispathsep_nocolon

# ── testing.c (not vendored) ──
# The Vim assert framework backing assert_equal()/assert_true()/… . Their
# implementations live in Neovim's testing.c, which is not part of the vendored
# eval tree; the spec is in csrc/eval.lua and v:errors is appended via the
# vendored assert_error() (csrc/eval/vars.c). These are faithful ports, cited to
# testing.c. The shared helpers (fill_assert_error, assert_equal_common,
# assert_match_common, assert_bool) are real Neovim functions from the same file.
f_assert_equal
f_assert_notequal
f_assert_true
f_assert_false
f_assert_match
f_assert_notmatch
f_assert_report
f_assert_inrange
f_assert_exception
fill_assert_error
assert_equal_common
assert_match_common
assert_bool

# ── message.c (not vendored) — emsg capture for assert_fails() ──
# emsg/semsg/did_emsg appear as calls in the vendored eval tree, so the
# generator recognizes them. These two model message.c's emsg_silent + the saved
# error list that testing.c's assert_fails() inspects; they have no single C
# function name (extension plumbing), so they are sanctioned here.
capture_errors_begin
capture_errors_take

# ── os/shell.c + os/env.c (not vendored) — system()/systemlist()/environ() ──
# Real Neovim builtins whose home files (os/shell.c, os/env.c) are outside the
# vendored eval tree; faithful std-only ports (shell out / read the environment).
f_system
f_systemlist
f_environ
get_cmd_output

# ── glob() — eval/funcs.c (this subset) + path.c (not vendored) ──
# f_glob lives in Neovim's eval/funcs.c, which this vendored funcs.c subset does
# not include; unix_expandpath is the real path.c wildcard walker behind it.
# expand_env is recognized already (it is called in the vendored eval/fs.c).
f_glob
unix_expandpath
f_strutf16len
f_utf16idx
utf_char2utf16len

# ── encode.c float rendering ──
# vim_float_g is the `%g` float-to-string rendering that encode.c's
# TYPVAL_ENCODE_CONV_FLOAT macro performs inline via vim_snprintf("%g", …)
# (Neovim has no named helper for it); faithful port of that formatting.
vim_float_g

# ── search.c (not vendored) — matchfuzzy()/matchfuzzypos() ──
# Real Neovim fuzzy matcher. Home file search.c is outside the vendored eval/
# tree; faithful ports cited to search.c.
f_matchfuzzy
f_matchfuzzypos
do_fuzzymatch
fuzzy_match
fuzzy_match_recursive
fuzzy_match_compute_score

# ── cmdhist.c (not vendored) — command-line history ──
# Real Neovim history rings behind histadd()/histget()/histnr()/histdel();
# get_histtype maps a history name to its ring index. Home file cmdhist.c.
f_histadd
f_histget
f_histnr
f_histdel
get_histtype

# ── digraph.c (not vendored) — digraph table ──
# Real Neovim digraph builtins; getexactdigraph resolves a two-char trigger.
f_digraph_get
f_digraph_set
f_digraph_getlist
f_digraph_setlist
getexactdigraph

# ── mbyte.c (not vendored) — iconv()/cell-width overrides ──
# Real Neovim string-encoding + cell-width-table builtins; cw_value looks up the
# setcellwidths() override consulted by utf_char2cells(). Home file mbyte.c.
f_iconv
f_setcellwidths
f_getcellwidths
cw_value

# ── eval/funcs.c (full table, not in the vendored funcs.c subset) ──
# Real Neovim builtins whose definitions sit in the parts of eval/funcs.c that
# this vendored subset does not include.
f_hostname
f_argc
f_argidx
f_argv
f_arglistid

# ── fold.c (not vendored) — foldlevel() (no folds standalone → 0) ──
f_foldlevel

# ── testing.c (not vendored) — assert_equalfile() ──
f_assert_equalfile

# ── window.c (not vendored) — match highlighting (w_match_head list) ──
# Real Neovim match builtins; match_dict_val wraps a dict_T as a VAR_DICT typval.
f_matchadd
f_matchaddpos
f_matchdelete
f_getmatches
f_setmatches
f_clearmatches
f_matcharg
match_dict_val

# ── sign.c (not vendored) — sign definitions ──
f_sign_define
f_sign_getdefined
f_sign_undefine

# ── fold.c (not vendored) — foldclosed()/foldclosedend() (no folds → -1) ──
f_foldclosed
f_foldclosedend

# ── mapping.c (not vendored) — mapping queries (no mappings standalone) ──
f_hasmapto
f_maparg
f_mapcheck
f_maplist

# ── ex_getln.c (not vendored) — command-line state ──
f_setcmdline
f_getcmdline
f_setcmdpos
f_getcmdpos
f_getcmdtype

# ── sign.c (not vendored) — placed-sign list ──
# sign_place_one is the shared placement core; sign_dict_nr/sign_dict_str read a
# Number/String field from a Dict argument (Neovim uses tv_dict_get_number/
# tv_dict_get_string for the same).
f_sign_place
f_sign_getplaced
f_sign_unplace
f_sign_placelist
f_sign_unplacelist
f_sign_jump
sign_place_one
sign_dict_nr
sign_dict_str

# ── editor-feature queries with a defined "no editor" answer ──
# indent.c / fold.c / highlight_group.c / diff.c / plines.c / cmdexpand.c /
# search.c / insexpand.c — each returns its documented absent-feature value.
f_indent
f_foldtext
f_foldtextresult
f_highlight_exists
f_diff_filler
f_virtcol2col
f_wildtrigger
f_searchcount
f_complete_info

# ── quickfix.c (not vendored) — quickfix/location lists ──
# qf_add_entry builds a normalized entry Dict (real Neovim function); qf_set_list
# / qf_get_list are the shared set/get cores behind set_errorlist/get_errorlist.
f_setqflist
f_getqflist
f_setloclist
f_getloclist
qf_add_entry
qf_set_list
qf_get_list

# ── cmdexpand.c / getchar.c / indent.c / insexpand.c / menu.c / eval.c ──
# getcompletion does real environment/file completion; the rest return their
# documented "no editor" values.
f_getcompletion
f_getchar
f_getcharstr
f_getcharmod
f_getcmdprompt
f_getcmdscreenpos
f_getcmdcompltype
f_getcmdcomplpat
f_cindent
f_lispindent
f_complete_add
f_complete_check
f_cmdcomplete_info
f_menu_info
f_test_garbagecollect_now
f_test_write_list_log

# ── round 5: completing the eval.lua builtin table ──
# Provider evals with no provider (v:null), undo-file path computation, and the
# remaining mouse/screen/completion/command-name/stacktrace queries. Home files:
# if_py.c / if_pyx.c / undo.c / mouse.c / screen.c / cmdexpand.c / insexpand.c /
# mapping.c / runtime.c / userfunc.c / ex_docmd.c — all outside csrc/eval/.
f_pyeval
f_pyxeval
f_undofile
f_undotree
f_getmousepos
f_screenpos
f_getcompletiontype
f_mapset
f_complete
f_preinserted
f_getscriptinfo
f_getstacktrace
f_fullcommand

# ── round 6: real mapping subsystem (mapping.c) ──
# An in-memory mapping table populated by mapset() and queried by maparg()/
# mapcheck()/maplist()/hasmapto(). These are the real mapping.c functions behind
# them (home file mapping.c, outside the vendored eval tree).
mode_str2flags
map_mode_to_chars
mapblock_fill_dict
map_add
getmaparg
get_map_mode
do_map

# ── round 7: user-defined commands (usercmd.c) ──
# `:command Name {repl}` stores a command; invoking `:Name args` expands and
# runs the replacement. Home file usercmd.c, outside the vendored eval tree.
uc_add_command
find_ucmd
uc_check_code
do_ucmd
ex_command
ex_delcommand

# ── round 8: autocommands (autocmd.c) ──
# `:autocmd {event} {pat} {cmd}` registers; `:doautocmd` fires; `:augroup` sets
# the group; exists('#event') queries. Home file autocmd.c (+ match_file_pat
# from fileio.c), outside the vendored eval tree.
do_augroup
do_autocmd
apply_autocmds
do_doautocmd
au_exists
match_file_pat

# ── round 9: in-memory current buffer (buffer.c / memline.c / eval-typval) ──
# A single virtual buffer backs getline()/setline()/append()/cursor()/line()/
# col()/… standalone. These are the real buffer.c helpers behind them, plus the
# String/List text-argument adapter (tv_lines_arg has no single C name).
curbuf_len
tv_get_lnum
get_buffer_lines
set_buffer_lines
set_cursorpos
tv_lines_arg

# ── round 10: buffer search (search.c) ──
# searchit is the real Neovim buffer-search core; line_match_from / finish_search
# are its per-line match + cursor-move helpers (no single C name).
searchit
line_match_from
finish_search
do_searchpair
skip_cols

# ── round 11: buffer-editing Ex commands (ex_cmds.c / ex_docmd.c) ──
# :[range]d/s/g/v/m/t/j/y/pu on the in-memory buffer. parse_addr/parse_line_range
# parse the line range; split_delim splits :s///:g// fields (no single C name).
do_excmd
parse_addr
parse_line_range
split_delim
ex_move
ex_copy
ex_join

# ── round 12: stream-editor Ex commands (ex_cmds.c / ops.c) ──
# :r/:w/:e file I/O, :sort, :>/:< shift, :!/:%! shell filter. ex_sort/ex_shift
# are real Neovim functions; shell_capture/shell_feed/shell_filter are the
# std-only shell-out adapters (no single C name).
ex_sort
ex_shift
shell_capture
shell_feed
shell_filter
