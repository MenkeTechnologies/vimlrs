// Vendored SUBSET of Neovim src/nvim/types_defs.h — only the opaque handle type
// the window model needs. Verbatim from upstream; trimmed to the subset.
#pragma once

// Opaque handle used by API clients to refer to various objects in vim
typedef int handle_T;
